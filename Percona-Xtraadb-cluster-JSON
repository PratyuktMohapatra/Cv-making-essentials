{
  "projectIdentity": {
    "name": "Percona XtraDB Cluster (PXC) Migration",
    "tagline": "High-Availability Database Clusterization for 570+ Mission-Critical Applications",
    "summary": "Designed and implemented a 3-node Percona XtraDB Cluster (PXC) to replace a legacy single-instance MySQL server. This migration eliminated single points of failure for an ecosystem of 570+ web applications. The architecture features advanced traffic segregation using HAProxy to split 'Execution' and 'Reporting' workloads, ensuring 24/7 uptime and high-performance throughput."
  },
  "technicalArchitecture": {
    "databaseSystem": {
      "software": "Percona XtraDB Cluster (PXC)",
      "topology": "3-Node Multi-Master Cluster",
      "nodes": [
        "103.114.154.243 (pxc1 - Primary Execution Node)",
        "103.114.154.244 (pxc2 - Reporting/Redundancy)",
        "150.242.14.145 (pxc3 - Reporting/Redundancy)"
      ],
      "replicationTechnology": "Galera Replication (wsrep) with Row-Based Replication (ROW) for synchronous consistency."
    },
    "loadBalancing": {
      "layer": "HAProxy (High Availability Proxy)",
      "setup": "Dual HAProxy Servers (Active-Passive)",
      "strategy": "Intelligent Traffic Segregation (Read/Write Splitting)",
      "logic": "Configured HAProxy to route real-time 'Execution DB' traffic specifically to Node 1 for low-latency writes, while heavy analytical 'Reporting DB' queries are routed to Nodes 2 and 3. This physical isolation prevents reporting queries from locking tables required by the live automation engine."
    },
    "infrastructure": {
      "operatingSystem": "Linux (CentOS/RHEL based on file paths)",
      "network": "Private Cluster Network (gcomm://) with split-brain protection (pc.weight=4, pc.ignore_sb=1)."
    }
  },
  "configurationHighlights": {
    "performanceTuning": {
      "innodb_buffer_pool_size": "4G (Optimized for RAM usage)",
      "max_connections": "6000 (To handle high concurrency from 570+ apps)",
      "innodb_flush_method": "O_DIRECT (Bypasses OS cache for direct disk I/O)",
      "wsrep_slave_threads": "8 (Parallel replication threads to prevent lag)",
      "innodb_log_file_size": "256M",
      "innodb_io_threads": "4 Read / 4 Write"
    },
    "clusterStability": {
      "pxc_strict_mode": "ENFORCING (Prevents experimental features from corrupting data)",
      "wsrep_sst_method": "xtrabackup-v2 (Non-blocking State Snapshot Transfer)",
      "gcache.size": "8G (Large Galera cache to support node recovery without full re-sync)",
      "wsrep_provider_options": "pc.weight=4; pc.ignore_sb=1 (Configuration to handle network partitioning and avoid split-brain)"
    },
    "securityAndNetworking": {
      "bind_address": "0.0.0.0 (Open binding for internal cluster communication)",
      "pxc_encrypt_cluster_traffic": "OFF (Disabled to maximize internal throughput speed)",
      "default_authentication": "mysql_native_password"
    }
  },
  "operationalMetrics": {
    "scale": "Supports 570+ Web Applications",
    "uptime": "99.99% Availability (High Availability)",
    "throughput": "Handles concurrent read/write operations for both real-time execution tracking and heavy historical reporting.",
    "improvements": [
      "Eliminated single point of failure (SPOF).",
      "Decoupled reporting loads from transactional loads.",
      "Enabled zero-downtime maintenance (rolling upgrades)."
    ]
  },
  "roleAndContribution": "I architected the entire migration plan, configured the `my.cnf` parameters for optimal PXC performance (tuning buffer pools, log capacities, and Galera settings), and set up the HAProxy routing logic to effectively split the Execution and Reporting database traffic."
}
